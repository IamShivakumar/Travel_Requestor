{
    "builds": [
      {
        "src": "main/wsgi.py",
        "use": "@vercel/python",
        "config": {
          "maxLambdaSize": "15mb",
          "runtime": "python3.12"
        }
      }
    ],
    
    "routes": [
      {
        "src": "/(.*)",
        "dest": "main/wsgi.py"
      }
    ],
    "installCommand": "sudo apt-get update && sudo apt-get install -y libpq-dev python3.12-dev build-essential && pip3.12 install --disable-pip-version-check --target . --upgrade -r requirements.txt",
    "buildCommand": "pip install -r requirements.txt"
  }